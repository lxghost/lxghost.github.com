# cSpell:ignore goldmark markmap plantuml readingtime
#
# baseURL moved to be after params
title: OINK
enableRobotsTXT: true
enableGitInfo: true
enableEmoji: true
defaultContentLanguage: en

ignoreLogs:
  # Project-documentation pages intentionally override theme parameters.
  - warning-frontmatter-params-overrides # cSpell:disable-line

# Allow iframe-hosted custom themes (for example, Giscus) to load during local previews.
# These headers are only used by `hugo server`; they do not affect generated production files.
server:
  headers:
    - for: /**
      values:
        Access-Control-Allow-Origin: '*'

outputs:
  # NAVJSON publishes navigation.json per language — the live demonstration
  # the agents guide links to.
  home: [HTML, markdown, LLMS, NAVJSON]
  page: [HTML, markdown]
  section: [HTML, RSS, print, markdown]

services:
  googleAnalytics:
    id: G-6JLQEHYFQG

imaging:
  resampleFilter: CatmullRom # cSpell:disable-line

markup:
  tableOfContents:
    endLevel: 4
  goldmark:
    parser:
      # Block images can carry an attribute line ({caption=…}, Book figures).
      wrapStandAloneImageWithinParagraph: false
      attribute:
        block: true
    extensions:
      passthrough:
        enable: true
        delimiters:
          block: [['\[', '\]'], ['$$', '$$']]
          inline: [['\(', '\)']]
    renderer:
      unsafe: true
  highlight:
    noClasses: false # Required for dark-mode

languages:
  en:
    label: English
    locale: en-US
    weight: 1
    title: OINK
    params:
      description: A Hugo theme for engineering docs
      comments:
        giscus:
          ariaLabel: Comments
          errorMessage: Comments could not be loaded. Please try again later.
      ui:
        navbar_autohide: true
        command_palette:
          commands:
            - id: theme_issues
              title: OINK issues
              description: Report or review theme and documentation issues
              url: https://github.com/pgsty/oink/issues
              icon: fa-brands fa-github
              keywords: [bug, support, roadmap]
    menus:
      main:
        - identifier: docs
          name: Docs
          pageRef: /docs
          weight: 20
        - identifier: docs-about
          parent: docs
          name: Introduction
          pageRef: /docs/about
          weight: 10
          params:
            icon: fa-solid fa-circle-info
            description: What OINK is, highlights, showcase, and licenses
        - identifier: docs-start
          parent: docs
          name: Get started
          pageRef: /docs/start
          weight: 20
          params:
            icon: fa-solid fa-rocket
            description: Clone this site, preview, make it yours, deploy
        - identifier: docs-write
          parent: docs
          name: Authoring
          pageRef: /docs/write
          weight: 30
          params:
            icon: fa-solid fa-pen-nib
            description: Pages, structure, blog, books, releases, API docs
        - identifier: docs-components
          parent: docs
          name: Components
          pageRef: /docs/components
          weight: 40
          params:
            icon: fa-solid fa-cubes
            description: One page per component, examples first
        - identifier: docs-customize
          parent: docs
          name: Customization
          pageRef: /docs/customize
          weight: 50
          params:
            icon: fa-solid fa-sliders
            description: Brand, navigation, search, languages, versions, print, agents
        - identifier: docs-admin
          parent: docs
          name: Operations
          pageRef: /docs/admin
          weight: 60
          params:
            icon: fa-solid fa-screwdriver-wrench
            description: Preview, deploy, comments, analytics, upgrade
        - identifier: docs-design
          parent: docs
          name: Design
          pageRef: /docs/design
          weight: 70
          params:
            icon: fa-solid fa-compass-drafting
            description: Maintainer contracts for architecture, components, shell, landing, and migration
        - identifier: book
          name: Book
          pageRef: /book
          weight: 30
        - identifier: case
          name: Case
          pageRef: /case
          weight: 40
        - identifier: blog
          name: Blog
          pageRef: /blog
          weight: 50
        - identifier: blog-oink
          parent: blog
          name: OINK engineering
          pageRef: /blog/oink
          weight: 10
          params:
            icon: fa-solid fa-screwdriver-wrench
            description: Design notes and engineering reports from the theme
        - identifier: blog-release
          parent: blog
          name: Releases
          pageRef: /blog/release
          weight: 20
          params:
            icon: fa-solid fa-tags
            description: Version announcements and migration notes
        - identifier: github
          name: GitHub
          url: https://github.com/pgsty/oink
          weight: 90
          params:
            icon: fa-brands fa-github

  zh:
    label: 简体中文
    locale: zh-CN
    weight: 2
    title: OINK
    hasCJKLanguage: true
    params:
      description: 为工程而设计的 Hugo 文档主题
      comments:
        giscus:
          ariaLabel: 评论
          errorMessage: 评论加载失败，请稍后重试。
      ui:
        navbar_autohide: true
        command_palette:
          commands:
            # Keep this order aligned with the English catalog: the palette
            # lists commands as configured, so a different order here would
            # give Chinese readers a different command sequence.
            - id: theme_issues
              title: OINK 问题反馈
              description: 报告或查看主题与文档问题
              url: https://github.com/pgsty/oink/issues
              icon: fa-brands fa-github
              keywords: [缺陷, 支持, 路线图]
    menus:
      main:
        - identifier: docs
          name: 文档
          pageRef: /docs
          weight: 20
        - identifier: docs-about
          parent: docs
          name: 简介
          pageRef: /docs/about
          weight: 10
          params:
            icon: fa-solid fa-circle-info
            description: OINK 是什么、亮点特性、示例站点与许可
        - identifier: docs-start
          parent: docs
          name: 快速上手
          pageRef: /docs/start
          weight: 20
          params:
            icon: fa-solid fa-rocket
            description: 克隆本站、本地预览、替换站点信息、部署上线
        - identifier: docs-write
          parent: docs
          name: 创作内容
          pageRef: /docs/write
          weight: 30
          params:
            icon: fa-solid fa-pen-nib
            description: 页面、目录、博客、书籍、发布页与 API 文档
        - identifier: docs-components
          parent: docs
          name: 组件
          pageRef: /docs/components
          weight: 40
          params:
            icon: fa-solid fa-cubes
            description: 一个组件一页，例子在前
        - identifier: docs-customize
          parent: docs
          name: 定制站点
          pageRef: /docs/customize
          weight: 50
          params:
            icon: fa-solid fa-sliders
            description: 品牌、导航、搜索、多语言、多版本、打印与 Agent 输出
        - identifier: docs-admin
          parent: docs
          name: 维护管理
          pageRef: /docs/admin
          weight: 60
          params:
            icon: fa-solid fa-screwdriver-wrench
            description: 本地预览、发布上线、评论、分析与升级
        - identifier: docs-design
          parent: docs
          name: 设计契约
          pageRef: /docs/design
          weight: 70
          params:
            icon: fa-solid fa-compass-drafting
            description: 面向维护者的架构、组件、阅读壳、落地页与迁移契约
        - identifier: book
          name: 教程
          pageRef: /book
          weight: 30
        - identifier: case
          name: 案例
          pageRef: /case
          weight: 40
        - identifier: blog
          name: 博客
          pageRef: /blog
          weight: 50
        - identifier: blog-oink
          parent: blog
          name: OINK 工程实践
          pageRef: /blog/oink
          weight: 10
          params:
            icon: fa-solid fa-screwdriver-wrench
            description: 主题设计记录与工程报告
        - identifier: blog-release
          parent: blog
          name: 版本发布
          pageRef: /blog/release
          weight: 20
          params:
            icon: fa-solid fa-tags
            description: 版本公告与迁移说明
        - identifier: github
          name: GitHub
          url: https://github.com/pgsty/oink
          weight: 90
          params:
            icon: fa-brands fa-github

params:
  # Version related parameters
  #
  # `version` identifies the published site variant (badge, welcome line,
  # version menu) and is not always a git ref. Content that needs a resolvable
  # release ref -- install commands, for example -- uses `tdVersion.latest`.
  tdVersion:
    latest: &tdLatestVers v0.8.0
  version: v0.8.0
  version_menu: v0.8.0
  version_menu_pagelinks: false
  # Populate to enable the navbar version menu, for example:
  versions: # []
    - name: '**Versions**'
    - version: *tdLatestVers
      kind: latest
      url: https://oink.pgsty.com

  hugoMinVersion: &hugoMinVersion 0.160.1
  productionURL: &productionURL https://oink.pgsty.com
  copyright:
    authors: >-
      [Ruohang Feng](https://vonng.com/en) ([@Vonng](https://github.com/Vonng))
      @ [PGSTY](https://pgsty.com/), [CC BY
      4.0](https://creativecommons.org/licenses/by/4.0)
    from_year: 2026
    to_year: present
  footer_center_info: 'Powered by [Oink](https://oink.pgsty.com)'
  github_repo: https://github.com/pgsty/oink.pgsty.com
  github_project_repo: https://github.com/pgsty/oink
  github_subdir: ''
  offline_search: true
  # Keep the search entry points functional in the local Hugo preview used for
  # visual and browser QA, not only in a production build.
  offline_search_on_serve: true
  offline_search_index: summary
  offline_search_summary_length: 70
  offline_search_max_results: 10
  page_width: normal
  comments:
    enable: true
    type: giscus
    giscus:
      repo: pgsty/oink.pgsty.com
      repoId: R_kgDOTzFZAg
      category: Announcements
      categoryId: DIC_kwDOTzFZAs4DDCm-
      mapping: pathname
      strict: 0
      reactionsEnabled: 1
      emitMetadata: 0
      inputPosition: bottom
      theme: auto
      loading: lazy
  ui:
    typography: technical
    # Site-wide backlinks: every page lists the pages that link to it, so the
    # documentation demonstrates the 0.8.0 feature it describes.
    backlinks: true
    # The blog index toolbar gets the form cycle (list, cards, table) beside
    # its feed button, on the root and on every child section index.
    blog_index_toggle: true
    # Section indexes (`_index.md` without `no_list`) render their children as
    # cards; a section can override with front matter `section_index: list`.
    section_index: cards
    # The blog index is the card grid rather than the row list, since every
    # post here carries section art. The featured-image mode and the share bar
    # are section decisions, so they live in the blog cascades instead.
    blog_index: cards
    blog_index_columns: 3
    # The right-rail cloud lists every declared taxonomy and has no exclude
    # switch, so `authors` and `series` get their own icons rather than the
    # generic fallback they would otherwise share.
    taxonomy_icons:
      categories: fa-solid fa-folder
      tags: fa-solid fa-tags
      authors: fa-solid fa-user-pen
      series: fa-solid fa-layer-group
    quick_links: [docs, blog]
    # Starter sites use the quieter group-only policy explicitly (theme
    # default: all).
    sidebar_icon_policy: groups
    page_context_menu:
      enable: true
      assistant_links: true
      links: []
    dark_mode: true
    sidebar_menu_compact: true
    sidebar_menu_foldable: true
    sidebar_root_enabled: true
    sidebar_root_menu: true
    sidebar_width_min: 200
    sidebar_width_max: 500
    sidebar_item_overflow: ellipsis
    sidebar_menu_truncate: 2000
    sidebar_expand_levels: 2
    feedback:
      enable: false
      reasons: true
    reading_time: false
  plantuml:
    enable: true
    svg_image_url: https://www.plantuml.com/plantuml/svg/
    svg: false
  print:
    toc: true
  markmap: true
  drawio:
    enable: true
    drawio_server: https://embed.diagrams.net/

baseURL: *productionURL

taxonomies:
  tag: tags
  category: categories
  # Declaring these two is the whole switch: `authors` turns the article head
  # into portraits and linked names and makes each term page an author profile;
  # `series` adds the reading-order strip above a post that names one. Neither
  # has a theme parameter -- see docs/customize/taxonomy/#authors.
  author: authors
  series: series

module:
  imports:
    - path: github.com/pgsty/oink
  hugoVersion:
    extended: true
    min: *hugoMinVersion
  mounts:
    # English sources and colocated .zh.md translations.
    - source: content
      target: content
      sites: { matrix: { languages: [en, zh] } }
